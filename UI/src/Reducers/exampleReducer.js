

const initialState = {
 examples: [
   "example"
 ]
}

const exampleReducer = (state = initiaState, action) => {
  switch (action.type) {
  
    case EXAMPLE: 
    
  }
}

export default exampleReducer;